# Architectures

Architecture packages compose the shared crates into deployable systems.

Currently implemented:

- [`centralized`](centralized/README.md): coordinator server, training client, volunteer launcher, local testnet, protocol crate, and integration-test harness.
