[toolchain]
# Single source of truth for the Rust toolchain across local dev, CI, and the
# Docker build. Bump deliberately; `cargo`/`rustup` install this automatically.
channel = "1.96.0"
components = ["rustfmt", "clippy"]
profile = "minimal"
